"""
require an input of the baits
obviously fragments will be different but shouldn't matter too much
can just use input from hic pro

interaction to sparse is the same
take matrix from there, skipping sparse to peaks. at that point we can get the rows from peaks directly. now we would have basically the data that chicago has.








"""